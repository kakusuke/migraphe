name: Create recommendation models table
target: main
dependencies:
  - 00_infra/005_grant_permissions
up: "Create recommendation_models table with id, name, type, config_json, trained_at, is_active"
down: "Drop recommendation_models table"
