name: Create shipping carriers table
target: main
dependencies:
  - 00_infra/005_grant_permissions
up: "Create shipping_carriers table with id, name, code, tracking_url_template, is_active"
down: "Drop shipping_carriers table"
